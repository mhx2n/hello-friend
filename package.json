{
  "name": "advanced-quiz-bot",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "node -e \"require('http').createServer((q,r)=>r.end('Python bot project — see advanced_quiz_bot.py')).listen(process.env.PORT||8080)\""
  }
}
